# =============================================================================
# .gitignore – Simple Password Manager Application
#
# This file tells Git which files and folders to exclude from the repository.
# Sensitive user data, local environment files, IDE settings and build
# artefacts are all excluded so that nothing private is ever pushed to GitHub.
# =============================================================================


# -----------------------------------------------------------------------------
# Python virtual environment
# The .venv folder contains all installed packages for this project.
# It is created locally with "python -m venv .venv" and must NOT be committed
# because it is large, machine-specific and reproducible via requirements.txt.
# -----------------------------------------------------------------------------
.venv/
venv/
env/
ENV/


# -----------------------------------------------------------------------------
# Python bytecode and cache
# Python generates these compiled files automatically at runtime.
# They are machine-specific and can always be regenerated, so they should
# never be tracked by version control.
# -----------------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.pyo
*.pyd


# -----------------------------------------------------------------------------
# PyInstaller build artefacts
# These folders and files are generated when packaging the application as a
# standalone executable.  They are large, platform-specific and reproducible
# from the source code, so they are excluded from the repository.
# -----------------------------------------------------------------------------
dist/
build/
*.spec


# -----------------------------------------------------------------------------
# Application backup folder
# The "backups/" folder is created inside the project directory by an older
# version of the application and may contain copies of encrypted or plaintext
# password data.  It must never be committed to a public repository.
# -----------------------------------------------------------------------------
backups/


# -----------------------------------------------------------------------------
# Temporary and backup files
# Temporary files created by the OS, editors, or the application itself
# (e.g. .tmp re-encryption helpers, .bak safety copies, .log rotation files).
# -----------------------------------------------------------------------------
*.tmp
*.bak
*.log
*.swp
*.swo
*~


# -----------------------------------------------------------------------------
# IDE and editor configuration folders
# These directories contain settings specific to a developer's local
# installation and should not be shared via the repository.
# -----------------------------------------------------------------------------

# PyCharm / IntelliJ IDEA
.idea/

# Visual Studio / Visual Studio Code
.vs/
.vscode/

# Qodo AI assistant
.qodo/

# Claude Code AI assistant
.claude/

# Sublime Text
*.sublime-project
*.sublime-workspace

# Spyder
.spyderproject.db
.spyproject/


# -----------------------------------------------------------------------------
# Python packaging and distribution artefacts
# Generated when building or installing Python packages locally.
# -----------------------------------------------------------------------------
*.egg-info/
.eggs/
*.egg
MANIFEST
pip-wheel-metadata/
.installed.cfg


# -----------------------------------------------------------------------------
# Testing and coverage artefacts
# Generated by pytest, coverage.py and similar tools during development.
# -----------------------------------------------------------------------------
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/


# -----------------------------------------------------------------------------
# macOS system files
# The .DS_Store file is created by macOS Finder and should not appear
# in a cross-platform repository.
# -----------------------------------------------------------------------------
.DS_Store
.AppleDouble
.LSOverride


# -----------------------------------------------------------------------------
# Windows system files
# Thumbnail caches and desktop configuration files generated by Windows
# Explorer should not be tracked by version control.
# -----------------------------------------------------------------------------
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/


# -----------------------------------------------------------------------------
# User data directory (outside the project folder – listed here for clarity)
# The application stores all sensitive data in the OS user-data directory:
#   Windows : %APPDATA%\PasswordManager\
#   Linux   : ~/.local/share/PasswordManager/
#   macOS   : ~/Library/Application Support/PasswordManager/
#
# This directory lives outside the project folder and is therefore never
# staged by Git.  No special rule is needed, but this comment documents
# exactly where sensitive files (data.txt.enc, salt.bin, keycheck.bin, …)
# are stored so future contributors know not to look for them here.
# -----------------------------------------------------------------------------
